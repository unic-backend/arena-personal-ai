version: 1.1.5

# Cache desactive : LibreChat gardait sa configuration en memoire, et un
# changement de nom ou de liste de modeles ne se voyait pas au redemarrage.
# Sur une installation locale a un seul utilisateur, relire ce fichier ne
# coute rien ; ne pas voir ses propres modifications coute une soiree.
cache: false

endpoints:
  custom:
    - name: "Usman"
      apiKey: "${USMAN_API_KEY}"
      baseURL: "http://host.docker.internal:8000/v1"
      models:
        # TROIS entrees, volontairement. Decision du proprietaire, 2026-08-26 :
        # « les utilisateurs ne connaissent pas des modeles, ils ouvrent le
        # premier qui apparait et pensent que c est celui qui fait tout ».
        #
        # Ce n est pas une liste tronquee : usman-chat atteint TOUTES les
        # capacites en lisant la demande — web, documents, video, navigation,
        # correction de bug, depot. Les autres noms restent servis par
        # /v1/models pour un client averti, mais ne sont plus proposes ici.
        #
        # `tests/test_configuration_clients.py` verifie que rien n est devenu
        # injoignable en raccourcissant ce menu.
        default:
          - "usman-chat"
          - "usman-coder"
          - "usman-video"
        fetch: false
      titleConvo: true
      modelDisplayLabel: "Usman"
