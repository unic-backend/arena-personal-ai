# Dependances directes d'Usman : uniquement ce que le code importe.
# L'environnement complet et fige se trouve dans requirements.lock.txt.
#   Installation courante    : pip install -r requirements.txt
#   Reproduction a l'identique : pip install -r requirements.lock.txt

# --- API et serveur (apps/backend/main.py, scripts/start.ps1) ---
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5          # monte le 04/09/2026 : browser-use 0.13.10 l'epingle
                          # desormais a cette version exacte (etait 2.12.5)
python-multipart==0.0.32          # requis par FastAPI pour UploadFile et Form

# --- Configuration ---
python-dotenv==1.2.2
PyYAML==6.0.3

# --- Modele local Ollama (core/models/ollama_provider.py) ---
httpx==0.28.1

# --- Outils ---
faster-whisper==1.2.1     # tools/audio/transcription_tool.py
ddgs==9.15.0               # tools/search/web_search_tool.py
browser-use==0.13.10      # tools/browser/browser_use_tool.py — monte le 04/09/2026 :
                          # 0.13.8 epinglait mcp==1.26.0 et click==8.3.1, tous deux avec
                          # des failles publiees. 0.13.10 les monte a 2.1.1 et 8.3.3.
langchain-openai==1.1.14  # ChatOpenAI utilise par browser-use — corrige PYSEC-2026-76 (SSRF).
                          # 1.1.9 exigeait openai<2.26 ; browser-use 0.13.10 epingle desormais
                          # openai==2.26.0, que 1.1.14 accepte (>=2.26.0,<3.0.0).
lightrag-hku==1.5.6       # tools/rag/lightrag_tool.py
pypdf==6.16.2             # tools/documents/reader.py — lecture des PDF. Monte le
                          # 04/09/2026 (5 failles publiees) ; epingle exactement par
                          # browser-use 0.13.10, comme reportlab ci-dessous.
python-docx==1.2.0        # tools/documents/reader.py — lecture des .docx
openpyxl==3.1.5           # tools/documents/reader.py — lecture des .xlsx
python-pptx==1.0.2        # tools/documents/reader.py — lecture des .pptx
pypdfium2==5.13.0         # tools/documents/reader.py — rendu d'une page PDF en image, pour l'OCR
pytesseract==0.3.13       # tools/documents/reader.py — OCR d'un PDF scanne. Exige le binaire
                          # tesseract (+ tesseract-ocr-fra) installe a part sur la machine.
qrcode==8.2               # scripts/lancer_arena.ps1 — le carre a scanner avec le
                          # telephone. Appele par `python -c` depuis PowerShell, donc
                          # invisible d'un scan d'imports : il a manque ici du jour ou
                          # le lanceur a ete ecrit au 02/09/2026, et le QR code n'a
                          # jamais marche chez personne.
reportlab==4.4.9          # agents/plaquiste/devis_pdf.py — devis a la charte UniC.
                          # Epingle exactement par browser-use 0.13.8 : demander 5.0.1
                          # rendait requirements.txt insoluble. Verifie le 2026-08-28,
                          # suite complete au vert avec 4.4.9.
graphifyy==0.9.53         # core/connectors/graphify.py — graphe structurel du depot
                          # (tree-sitter, hors ligne). Nom du paquet PyPI : "graphifyy"
                          # (deux "y"), commande installee : "graphify". Extra [pdf]
                          # volontairement PAS installe : l'ingestion de documents dans
                          # le graphe n'est pas branchee ici (DEC-0046).
gitingest==0.3.1          # core/connectors/gitingest.py — un depot (local ou une URL)
                          # transforme en resume+arbre+contenu (DEC-0047). Extra [server]
                          # volontairement PAS installe : ARENA est deja le serveur, aucun
                          # besoin du serveur web/CLI autonome de GitIngest.
txtai_minimal==9.13.0     # core/connectors/txtai_search.py — recherche semantique
                          # explicite, jamais un second RAG (DEC-0051). Nom du paquet PyPI :
                          # "txtai_minimal" — la variante officielle SANS torch/transformers/
                          # faiss : le vecteur vient d'Ollama (embeddings_ollama, deja
                          # utilise par core/memory/semantique.py), jamais un second modele.
numpy==2.4.6              # core/production/txtai_recherche.py — le backend numpy de
                          # txtai_minimal (faiss absent) ; txtai_minimal ne declare aucune
                          # dependance dure (verifie : METADATA vide), donc l'import direct
                          # doit etre declare ici. Mesure le 05/09/2026 : absent de
                          # requirements.txt faisait planter la suite offline en CI
                          # (ModuleNotFoundError), invisible en local ou numpy arrivait
                          # deja transitivement par d'autres paquets.
ifcopenshell==0.8.5       # core/connectors/ifc.py — lecture BIM/IFC (DEC-0053).
                          # LGPL-3.0-or-later, wheels precompiles (aucun compilateur requis).
                          # Bibliotheque Python appelee, jamais du code copie.
Pillow==12.0.0            # core/production/conversion/moteurs.py — conversion d'images
                          # (DEC-0074). MIT-CMU. Absente du depot avant cette mission : aucune
                          # capacite ARENA ne convertissait une image jusqu'ici.
weasyprint==68.1           # core/production/conversion/moteurs.py — HTML/Markdown/TXT -> PDF
                          # (DEC-0074). BSD-3-Clause. Exige libcairo/libpango SYSTEME (mesure
                          # presents dans l'image de ce depot) ; absents, `sonder()` rapporte
                          # NON_CONFIGURE au lieu de planter au demarrage.
Markdown==3.10.3          # core/production/conversion/moteurs.py — Markdown -> HTML, avant
                          # WeasyPrint (DEC-0074). BSD-3-Clause.
CairoSVG==2.9.0            # core/production/conversion/moteurs.py — SVG -> PNG/PDF (DEC-0074).
                          # LGPL-3.0-or-later (meme regime qu'ifcopenshell ci-dessus) :
                          # bibliotheque Python appelee, jamais du code copie. Exige libcairo
                          # SYSTEME ; absente, `sonder()` le rapporte au lieu de planter.

# Non liste ici, et volontairement :
# - ffmpeg : binaire systeme, pas un paquet pip (voir Dockerfile)
# - libreoffice (soffice) : binaire systeme, pas un paquet pip — DOCX/PPTX/XLSX <-> PDF
#   (DEC-0074, core/production/conversion/moteurs.py). Absent, `sonder()` le rapporte.
# - sympy, numpy : installes dans l'image du bac a sable (tools/code/sandbox_image)
# - pywin32 : plus aucun import dans le code ; il faisait echouer le build Docker Linux
# - git-filter-repo : outil en ligne de commande, pas une dependance d'execution
